# Data contract for the jaffle_shop `customers` mart.
# GENERATED by scripts/generate_contract.py (docs/tutorials/odcs-python.md)
# from the live DuckDB catalog — do not edit by hand, regenerate instead.
version: 1.0.0
kind: DataContract
apiVersion: v3.1.0
id: 7fb49248-5e36-486d-a110-d7edcf2b2e84
name: customers
tenant: JaffleShopInc
tags:
- jaffle-shop
- customers
status: active
servers:
- server: local-duckdb
  type: duckdb
  description: Local DuckDB file built by setup_jaffle_shop.sql.
  environment: dev
  database: jaffle_shop.duckdb
  schema: main
dataProduct: jaffle_shop
description:
  usage: Customer segmentation, retention analysis, and lifetime-value reporting.
  purpose: One row per customer with rolled-up order history (first/latest order,
    order count, lifetime value).
  limitations: Includes customers with zero orders; date columns are null for them.
    Amounts are in USD.
domain: jaffle-shop
schema:
- name: customers
  physicalType: table
  description: Dimension table with one row per customer and their order history rolled
    up.
  businessName: Jaffle Shop customers
  physicalName: customers
  dataGranularityDescription: One row per customer (customer_id is unique).
  properties:
  - name: customer_id
    physicalType: INTEGER
    description: Customer primary key, carried over from raw_customers.id.
    primaryKey: true
    primaryKeyPosition: 1
    logicalType: integer
    required: true
    unique: true
  - name: first_name
    physicalType: VARCHAR
    description: Customer first name.
    primaryKey: false
    logicalType: string
    required: true
    unique: false
  - name: last_name
    physicalType: VARCHAR
    description: Customer last initial (anonymized, e.g. "P.").
    primaryKey: false
    logicalType: string
    required: true
    unique: false
  - name: first_order
    physicalType: DATE
    description: Date of the customer's first order; null if they never ordered.
    primaryKey: false
    logicalType: date
    required: false
    unique: false
  - name: most_recent_order
    physicalType: DATE
    description: Date of the customer's latest order; null if they never ordered.
    primaryKey: false
    logicalType: date
    required: false
    unique: false
  - name: number_of_orders
    physicalType: INTEGER
    description: Lifetime count of orders placed (0 for customers with no orders).
    primaryKey: false
    logicalType: integer
    required: true
    unique: false
  - name: customer_lifetime_value
    physicalType: DECIMAL(10,2)
    description: Lifetime sum of order totals in USD (0.00 for customers with no orders).
    primaryKey: false
    logicalType: number
    required: true
    unique: false
team:
  name: jaffle-analytics
  description: Analytics engineering team owning the jaffle shop marts.
  members:
  - username: alice@jaffleshop.example
    name: Alice Ordway
    role: owner
